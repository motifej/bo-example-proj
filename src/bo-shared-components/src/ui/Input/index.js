import React from "react";
import { Default, Disabled, createTextField } from "./inputs";

export const DisabledInput = Disabled;
export const DefaultInput = Default;
export const TextField = createTextField;
